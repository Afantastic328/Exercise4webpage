# AI4SE 实践反思

## 背景

本项目（校园需求墙）有两个版本：

1. **vanilla 版本**：用原生 HTML/CSS/JS 手写
2. **Vue 版本**：借助 AI（OpenCode）辅助完成，使用 Vue 3 + Vite 框架

本文反思在 Vue 版本开发中，AI 辅助开发的实践体验。

---

## 我在项目中实际做了什么

### 扮演的角色

- **产品经理**：提出需求（页面布局、视觉风格、交互逻辑）
- **验收测试**：运行项目、发现 bug、验证功能是否符合预期

### 具体操作

1. 描述想要的功能（如"卡片要有深紫色刘海"、"接单后要限制其他卡片"）
2. 确认 AI 提供的设计方案
3. 运行 `npm run dev` 查看效果
4. 发现问题并反馈（如"接单后发布按钮也被禁用了"）
5. 逐步迭代直到满意

---

## 我学到了什么

### 架构层面（理解了）

- Vue 项目为什么不能直接打开 HTML，需要 `npm run dev`（Vite 充当翻译官）
- 组件化思想：把页面拆成独立的小块，各自负责各自的事
- 父子组件通信：Props 是父传子，Emits 是子传父
- 响应式概念：数据变了，页面自动更新

### 语法层面（看到了，但不熟练）

- `v-for`：循环渲染列表
- `v-if`：条件显示不同内容
- `v-model`：双向数据绑定
- `ref`、`computed`：响应式数据

### 工程常识（掌握了）

- 项目文件结构各部分的职责
- mock API 的作用（用假数据模拟后端）
- 构建工具（Vite）的角色

---

### 核心问题：AI 到底帮我们多少是合适的？

实现"能用 AI 做出一个项目"这个层次其实并不困难，但在整个过程中，我一直有一个挥之不去的困惑：**AI 到底帮我们多少是合适的？**

在前期设计写 Spec 阶段，我不懂技术细节，也不懂 HTML、CSS、JS 的语法，面对 AI 提出的方案只能一直点 confirm——我甚至不确定自己是否真正理解了那些方案。等到代码生成出来之后，我也只能根据页面的样子向 AI 描述改进需求，无法自己动手修改或维护代码。

特别是对于我们刚刚起步的软件学生而言，这个问题更加突出：

- **应该如何利用 AI？** 是让它全权代劳，还是只让它辅助某一部分？
- **如何评价自己是否掌握？** 如果标准是"能做出一个项目"，那我确实做到了；但如果标准是"能独立写出来"，那我显然没有达到。

### 三种可能的"掌握程度"

| 层次         | 描述                | 我的现状  |
| ---------- | ----------------- | ----- |
| 1. 能提需求并验收 | 像产品经理一样使用 AI      | ✅ 达到  |
| 2. 能独立复现   | 没有 AI 也能写出来       | ❌ 未达到 |
| 3. 能改进和优化  | 能读懂代码、修复 bug、扩展功能 | ❌ 未达到 |

---

## 我的疑惑

1. 对于软工II这门课来说，我们现阶段的学习目标到底是能够靠AI做出来，还是说以真正掌握web前端开发为目标，能够脱离AI自己复现甚至改进？

2. 在使用AI做作业的过程中，多少帮助算是合理的，有哪些部分应该尝试自己完成？

3. 关于这门课的课程作业，有一部分是大的团队项目作业。根据学长的描述，我了解到在选题方面有比较严格的要求，请问今年的要求是什么？方便我们进行实地调研研究项目主题，以及和其他专业的同学合作探索展开跨学科的尝试。


